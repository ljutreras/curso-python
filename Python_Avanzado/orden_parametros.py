def parametros_ordenados(nombre,apellido,*args,**kwargs):
    pass

""" def parametros_desordenados(nombre,apellido,**kwargs,*args): #En este caso nos da error ya que **kwargs se encuentra antes que *args
    pass """

#Hay que respetar el orden de los parametros, en este caso...
#def funcion (Parametros de la funcion, *args, **kwargs)